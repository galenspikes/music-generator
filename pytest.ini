[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
# Put the repo root on sys.path so `pytest` (bare console script, as run in CI)
# finds the top-level modules, not just `python -m pytest`.
pythonpath = .
